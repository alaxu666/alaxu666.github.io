在爬取PKR数据.py中，实现以下功能
打开页面：https://plmcnprdawc.yanfeng.com:3000/#/showHome
定位input[name='username']，输入用户名“uxuxs004”
定位input[placeholder='Password']，输入密码“ABCabc%123”
点击登陆按钮：button.sw-button.accent-caution
验证页面中存在icon-id='homeEdit'表示登陆成功了。
点击title='XSO & PKR'的div，实现跳转
定位到id="iframeContent"，展开//span[text()='XSO Management']/parent::a元素，
点击//span[text()='Program Dashboard']/parent::a元素
定位到class="iframe_tab_menu-1-1"的iframe中，在这个iframe中执行以下操作
点击name="select_bu"的select标签，在下拉菜单中，选择value="DFM"的option
点击name="select_Category"的select标签，在下拉菜单中，选择value="S"的option
点击placeholder="Product Group"的input框，在里面输入“equipment"
点击onclick="searchproject()"的button
点击onclick="exportData()"的button，完成下载。

先不要关闭网页
用pandas导入C:\Users\uxuxs004\Downloads目录下，最新下载的Project_List开头的xls文档，Project_List因重名，可能会有很多带编号后缀的Project_List，查找并打开最新修改日期的。
筛选出Category是"S"，且Phase 1 Gate Exit-GO，Phase 2 Gate Exit-DVR，Phase 3 Gate Exit-FPR，Phase 4 Gate Exit-CPA日期在本周和下周内的数据，筛选后的表格名为df1.
在df1表格中，增加一列，列名为"当前状态"，如果Phase 1 Gate Exit-GO的日期在当前周或下周，则"当前状态"填入Phase 1 Gate Exit-GO；如果Phase 2 Gate Exit-DVR的日期在当前周或下周，则"当前状态"填入Phase 2 Gate Exit-DVR；如果Phase 3 Gate Exit-FPR的日期在当前周或下周，则"当前状态"填入Phase 3 Gate Exit-FPR；如果Phase 4 Gate Exit-CPA的日期在当前周或下周，则"当前状态"填入Phase 4 Gate Exit-CPA；
在df1表格中，再增加一列，列名为"PKR信息"

回到网页
定位到id="iframeContent"，展开//span[text()='PKR Management']/parent::a元素，
点击//span[text()='Program PKR Summary']/parent::a元素
定位到class="iframe_tab_menu-2-2"的iframe中，在这个iframe中执行以下操作


对所有df1表格中的Project ID进行循环，循环体开始：
定位id="searchProject"的input框，输入当前循环的Project ID
点击onclick="searchproject()"的button。
点击class="lbl"的span
如果当前Project ID对应的"当前状态"列数据是Phase 2 Gate Exit-DVR，搜索所有aria-describedby="result-table_dvr_html"的td标签。
	否则如果当前Project ID对应的"当前状态"列数据是Phase 3 Gate Exit-FPR，搜索所有aria-describedby="result-table_fpr_html"的td标签。
	否则如果当前Project ID对应的"当前状态"列数据是Phase 4 Gate Exit-CPA，搜索所有aria-describedby="result-table_cpa_html"的td标签。
		完成以上条件语句后，依次在这些<td>标签体中，查找class="div_score"的div。如果这个div中，存在onclick属性值，则提取这个div的onclick属性值中，最后一个逗号后面的字符串，再从这些字符串中，提取"'"字符和"_"字符中间的字符串，并将此字符串中的"%20"替换为" "，完成处理后的字符串命名为PKRname，如果这个div中不存在onclick属性值则PKRname赋值为字符串"Tolal"。提取这个div体中的文本，并命名为PKRscore。组合PKRname和PKRscore字符串，中间用加上","字符串，末尾加上换行符，放到PKRinfo字符串变量中。
把PKRinfo变量存到df1中对应数据的"PKR信息"列中。
循环体结束。

在df1中，增加一列，列名为“id”，“id”自动编号，不允许有重复。
对所有df1表格中的id列进行循环，循环体开始：
定位id="searchProject"的input框，输入当前循环的Project ID
点击onclick="searchproject()"的button。
点击class="lbl"的span
如果当前Project ID对应的"当前状态"列数据是Phase 2 Gate Exit-DVR，搜索所有aria-describedby="result-table_dvr_html"的td标签。
	否则如果当前Project ID对应的"当前状态"列数据是Phase 3 Gate Exit-FPR，搜索所有aria-describedby="result-table_fpr_html"的td标签。
	否则如果当前Project ID对应的"当前状态"列数据是Phase 4 Gate Exit-CPA，搜索所有aria-describedby="result-table_cpa_html"的td标签。
		完成以上条件语句后，依次在这些<td>标签体中，查找class="div_score"的div。如果这个div中，存在onclick属性值，则提取这个div的onclick属性值中，最后一个逗号后面的字符串，再从这些字符串中，提取"'"字符和"_"字符中间的字符串，并将此字符串中的"%20"替换为" "，完成处理后的字符串命名为PKRname，如果这个div中不存在onclick属性值则PKRname赋值为字符串"Tolal"。提取这个div体中的文本，并命名为PKRscore。组合PKRname和PKRscore字符串，中间用加上","字符串，末尾加上换行符，放到PKRinfo字符串变量中。
把PKRinfo变量存到df1中对应数据的"PKR信息"列中。
循环体结束。


confirm信息的提取补充：
在对所有df1表格中的Project ID进行循环之前，在df1表格中增加一列，列名叫Confirmed。
在df1表格中Project ID的循环体中，需要增加confirm信息的提取，提取方式如下：在每次完成提取PKRscore之后，在提取PKRscore的<div></div>体的前面（紧邻div体），有一个<i></i>体，提取这个<i>体中的title属性值，并把这个属性值放到df1新增的列（confirmed）中。最后所有输出excel表格，都要增加confirmed列。


在对所有df1表格中的Project ID进行循环之前，在df1表格中增加一列，列名叫“是否确认”。
在df1表格中Project ID的循环体中，每次循环结束前，提取confirmed列中的信息，如果在每个“PKRname,”后都是“Confirmed”，则在df1中的“是否确认”列，输入“完成确认”，否则输入“未完成”。

把所有PKRname去重，格式化后放到一个单元格中：
完成df1的所有信息输入后，在df1中增加一列，列名叫“未完成人名”
把所有“PKR信息”列中，除了Total以外，其他PKRname，去重后，通过“姓1,名1@yanfeng.com,姓2,名2@yanfeng.com,...”的格式，组合在一起，并放在df1的第1行数据的“未完成人名”。
把所有“PKR信息”列中，除了Total以外，其他PKRname，去重后，通过“姓1,名1,姓2,名2,...”的格式，组合在一起，并放在df1的第2行数据的“未完成人名”。
把所有“PKR信息”列中，除了Total以外，其他PKRname，去重后，通过“姓1,名1”的格式，放在df1的第3行数据的“未完成人名”，通过“姓2,名2”的格式，放在df1的第4行数据的“未完成人名”，以此类推，通过“姓n,名n”的格式，放在df1的第n+2行数据的“未完成人名”。
发邮件前，对于“是否确认”列中，不是“完成确认”的数据，收集这些数据行中，所有“未完成PKR信息”列中的所有PKRname（比如PKRname是“Xu Shirong”，则姓是“Xu”，名是“Shirong”;PKRname中如果存在“Wang Hao23”，则先把它替换为“Wang23 Hao”），对PKRname去重，假设去重后有n个PKRname，通过“名1.姓1@yanfeng.com; 名2.姓3@yanfeng.com; ...名n.姓n@yanfeng.com”的格式组成字符串，在第一行数据中的“未完成人名”列中，填入这个字符串。


shirong,xu@yanfeng.com,kailiang,zhao@yanfeng.com

我想把project name发给这一行数据中，PKR信息中出现的所有人，用teams发送，发送前先把人名每一行的人名转换成邮箱地址格式，格式与“Confirmed”列的第一行数据相同。
30030290-EBP
30030289-EBP



定位到id="iframeContent"，展开//span[text()='XSO Management']/parent::a元素，
点击//span[text()='Program Dashboard']/parent::a元素
定位到class="iframe_tab_menu-1-1"的iframe中，在这个iframe中执行以下操作
重新对EBP Leader为空值的Project ID进行循环，循环体开始：
定位id="searchProject"的input框，输入当前循环的Project ID
点击onclick="searchproject()"的button。
找到aria-describedby="grid-table_real_pm_name"的<td>标签，提取标签中的title属性值，并保存到EBP Leader中。
循环体结束。
保留df1中的下面这些列数据，其余的列全部删除：Project ID，Project Name，OEM Name，EBP Leader，Product Engineer，SMTE，Project State，Phase 1 Gate Exit-GO，Phase 2 Gate Exit-DVR，Phase 3 Gate Exit-FPR，Phase 4 Gate Exit-CPA，Phase 5 Gate Exit-PLR
增加一列，名为“未完成PKR信息”，分析一下df1中“PKR信息”列数据，如果存在PKRscore=0（每一行","后面的数字），则把这一行数据（PKRname,PKRscore）添加到“未完成PKR信息”，如果存在PKRscore≠100（每一行","后面的数字），则把这一行格式化后的数据数据（PKRname,PKRscore）添加到“未完成PKR信息”，如果一个“PKR信息”数据中，存在多行PKRscore≠100，则将这些数据都放到“未完成PKR信息”，分多行显示。如果“PKR信息”数据中全部的PKRscore都是100，则在“未完成PKR信息”中填入“完成”

导出df1到“C:\Users\uxuxs004\OneDrive - Yanfeng\work\产品安全\SSO\pythonProject\”目录下，并命名为“PKR完成情况（近两周）”

修改判定依据改为confirmed信息：
目前对“未完成PKR信息”的判定依据是PKRscore是否等于0。这个判定依据需要修改一下，修改为判断“是否确认”信息，如果“是否确认”是“完成确认”，则在“未完成PKR信息”输入“完成”，否则输入“未完成”。


增加未完成历史记录：
最后输出的PKR完成情况中，数据根据日期从早到晚排序后再输出exce文件。另外，把所有“未完成PKR信息”字段中，显示非“完成”的项目保存到PKR历史未完成记录.xlsx中。在前面完成本周和下周的数据筛选后，在df1表格中，增加PKR历史未完成记录.xlsx中的数据，把这些数据也进行PKR信息的爬取，在df1中增加1列，列名叫过门周，如果是本周过门，则显示“本周”，如果是下周过门，则显示“下周”。完成PKR数据爬取后，如果历史数据（非本周或下周数据）中，未完成PKR信息显示已完成的话，则把这条数据从PKR历史未完成记录.xlsx删除。最后更新覆盖PKR历史未完成记录.xlsx。

增加未完成历史记录的判定标准，不要从“未完成PKR信息”字段中判断，改为从“是否确认”中，显示非“完成”的项目保存到PKR历史未完成记录.xlsx中


PKR数据爬取.py的运行中，发现了一个问题，同一Project ID可能存在两个（或更多）不同的phase（Phase 1 Gate Exit-GO/Phase 2 Gate Exit-DVR/Phase 3 Gate Exit-FPR/Phase 4 Gate Exit-CPA/Phase 5 Gate Exit-PLR）日期同时在本周和下周内，对于这种情况，需要创建两行数据分别存放这两个数据，在“当前状态”列区分这两个数据的区别，后面循环爬取PKR信息时，依次爬取这个Project ID的不同phase的PKR信息。


PKR数据爬取.py中，能不能在代码全部执行后，用当前登陆的teams，把所有“是否确认”列中显示“未完成”的数据中，“Project Name”列和“当前状态”列，以文本表格的形式，发给shirong.xu@yanfeng.com账号。

测试后发现浏览器中打开的表格显示正常的表格，邮件窗口中显示的还是纯本吧表格。但是我手动选中浏览器中的的表格，通过复制粘贴到邮件窗口中的话，邮件窗口中能够正常显示美化后的html表格。能不能通过自动复制粘贴的方式，在邮件窗口中显示html表格呢？
另外最后发送邮件的表格中，增加一列“未完成PKR信息”，三列顺序是“Project Name”，“当前状态”，“未完成PKR信息”。发送人框中输入“未完成人名”列中的第一行文本。抄送框中输入liang.cao@yanfeng.com。

Lu.Gongchao@yanfeng.com,Zhao.Kailiang@yanfeng.com
Gongchao.lu@yanfeng.com; Kailiang.Zhao@yanfeng.com; shirong.xu@yanfeng.com; hao.wang23@yanfeng.com




shirong.xu@yanfeng.com
收件人不要输入shirong.xu@yanfeng.com，要输入“未完成人名”列中的第一行文本，比如Lu.Gongchao@yanfeng.com,Zhao.Kailiang@yanfeng.com。
保持其他代码不变，在输出“未完成人名”的第一行数据后，查找数据中是否存在“Wang.Hao23”，如果有这个字符串，则把这段字符串替换为“hao.wang23“。
【另外“是否确认”列，如果“未完成PKR信息”显示“完成”，但“Confirmed”列PKRname后没有全部confirmed，则在“是否确认”列显示“未确认(cao liang)”；如果“未完成PKR信息”显示非“完成”，则不管“Confirmed”列显示什么，都在“是否确认”列显示“未递交(工程师)”。最后在邮件html表格中，最后增加一列“是否确认”。4列顺序是“Project Name”，“当前状态”，“未完成PKR信息”，“是否确认”。】被下面代替：
如果“Confirmed”列数据中的每一行PKRname后的“,”后的文本，全部是“Confirmed”，则在这个数据对应的“是否确认”列填入“完成确认”。如果“Confirmed”列数据中的每一行PKRname后的“,”后的文本，全部是“Submitted”，则在这个数据对应的“是否确认”列填入“未确认(Cao liang)”。如果“Confirmed”列数据中的每一行PKRname后的“,”后的文本，只要有非“Confirmed”且非“Submitted”的数据存在，则在这个数据对应的“是否确认”列填入“未评分(工程师)”。



邮件中不要显示文本表格了，我手动从浏览器中复制粘贴html表格。

通过邮件发送的哪些数据。也就是“是否确认”列中，不是“完成确认”，且“当前状态”列不是“Phase 1 Gate Exit-GO”的数据进行导出覆盖“PKR未完成情况（近两周）.xlsx”
把PKR未完成情况同步到飞书.py修改成一个可调取的模块，在PKR数据爬取运行到最后，自动调取PKR未完成情况同步到飞书.py中的函数，完成本地数据替换飞书数据。

PKR信息列中的数据，把“Total”改为“最低打分：”，把PKRname和PKRscore中间的“,”替换为“的PKR打分：”，在每个PKRscore后面，增加“；”
修改“PKR信息”列的格式后，“未完成PKR信息”列的输出发生了偏差，按照下面的要求，重新修改代码：等待df1中“PKR信息”列完成数据采集并格式化后，分析一下“PKR信息”列数据，如果存在PKRscore≠100（每一行","后面的数字），则把这一行格式化后的数据数据（PKRname,PKRscore）添加到“未完成PKR信息”，如果一个“PKR信息”数据中，存在多行PKRscore≠100，则将这些数据都放到“未完成PKR信息”，分多行显示。如果“PKR信息”数据中全部的PKRscore都是100，则在“未完成PKR信息”中填入“完成”

config.py中，保存了一些敏感信息，我不想放在git仓库，每次都上传到github中，所以把路径改到了本地的其他目录内，在git仓库中放了config_DIR.py，其中有config.py的本地路径，读取这个路径后，在程序中导入这个路径下的配置文件config.py。帮我检查本仓库中其他文件是否也有引用config.py的程序，把代码全部都修改掉。



把PKR数据爬取.py中的网页登陆部分代码，打包成函数放到YanfengAutoWork.py中，函数名叫PLMLogin，以后直接调用这个函数就能完成登陆。登陆完成后，下载Project_List部分代码，打包成名为DL_Project_List的函数，也放到YanfengAutoWork.py中，程序直接调用这个函数完成下载。读取最新修改的Project_List?.xls部分代码，也打包成名为RD_Project_List的函数，放在YanfengAutoWork.py中，函数的返回值是pandas的frame类型，存放Project_List的数据。

把PKR数据爬取.py中，发现EBP Leader空值的数据，去PLM系统爬取数据的那部分代码，打包成函数放到YanfengAutoWork.py中，函数名叫FindEBPLeader，函数功能是：传入一个由Project ID组成的列表数据，函数功能是完成从PLM系统中提取EBPLeader数据，并返回一个frame格式的列表，列表第一列Project ID，第二列是网页中查到的EBPLeader数据，函数功能描述如下（这段代码需要放在完成PLMLogin后）：
定位到id="iframeContent"，展开//span[text()='XSO Management']/parent::a元素，
点击//span[text()='Program Dashboard']/parent::a元素
定位到class="iframe_tab_menu-1-1"的iframe中，在这个iframe中执行以下操作
重新对EBP Leader为空值的Project ID进行循环，循环体开始：
定位id="searchProject"的input框，输入当前循环的Project ID
点击onclick="searchproject()"的button。
找到aria-describedby="grid-table_real_pm_name"的<td>标签，提取标签中的title属性值，并保存到EBP Leader中。
循环体结束。
把PKR数据爬取.py这部分代码，替换成引用YanfengAutoWork.py中的FindEBPLeader函数实现。
最后如果导出excel失败，比如文件被打开后锁住了。则跳出提示词，“文件已被锁定，尝试关闭文件后，按回车键再试”
